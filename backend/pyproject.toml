[project]
name = "sentinel-ai-backend"
version = "1.0.0"
description = "SentinelAI Mission Control — Backend API"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py312"
